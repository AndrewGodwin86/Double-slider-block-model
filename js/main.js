// Push initial values to input fields
$('document').ready(function(){
	var alphaInput = document.getElementById('alpha');
	alphaInput.value = alpha;

	var betaInput = document.getElementById('beta');
	betaInput.value = beta;

	var phiInput = document.getElementById('phi');
	phiInput.value = phi;

	var numSlipEventsInput = document.getElementById('numberOfSlipEvents');
	numSlipEventsInput.value = nn;

	var y1InitVal = document.getElementById('y1Init');
	y1InitVal.value = y1;

	var y2InitVal = document.getElementById('y2Init');
	y2InitVal.value = y2;	

});

